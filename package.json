{
  "name": "ydk_delta_seek_gui",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "rm -rf ./serve_temp && transcrypt ./python/tool.py -od ../src/transcrypt && parcel serve ./src/index.html --dist-dir serve_temp",
    "build": "npm upgrade && rm -rf ./dist && transcrypt ./python/tool.py -od ../src/transcrypt && parcel build ./src/index.html"
  },
  "browserslist": [
    "last 2 versions",
    "> 1%",
    "not dead"
  ],
  "author": "Aditya Kishore",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.0.0-beta.3.1"
  },
  "dependencies": {
    "bulma": "^0.9.3",
    "preact": "^10.5.14"
  }
}
